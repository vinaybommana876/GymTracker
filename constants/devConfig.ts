/**
 * Development Configuration
 * Toggle these flags for testing and debugging
 */

// 🔄 Reset onboarding on every app start (useful for testing the onboarding flow)
export const RESET_ONBOARDING_ON_START = true;

// 📱 Enable console logging for debugging
export const DEBUG_ONBOARDING_FLOW = true;
